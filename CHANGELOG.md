# Changelog

## 0.2.0 - 2026-02-25

- Added `TextFrame.html` extension API (getter + setter) via `install_html_extension()`.
- Auto-installs extension on `import pptx_html_generator`.
- Added extension test coverage (`tests/test_extension.py`).
- Added nested/overlapping HTML parser edge-case tests.
- Updated public docs (`README.md`, `docs/API.md`) for extension usage.

